name: Deploy to Netlify

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify build
        run: |
          curl -s -X POST "https://api.netlify.com/build_hooks/6a3e55b6ddab0f0adee165e6"
